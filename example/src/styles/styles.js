import React from 'react';
import {
    StyleSheet,
} from 'react-native';

class code {
    static styles = StyleSheet.create({
        container: {
            flex: 1,
            flexDirection: 'column',
            backgroundColor: '#eeeceb'
        },
    });
}